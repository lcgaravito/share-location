export interface MarkerType {
  id: string;
  latitude: number;
  longitude: number;
  color: string;
  name: string;
  direction: string;
}

export interface Location {
  latitude: number;
  longitude: number;
}
